# Database Configuration
# This file shows the required environment variables for @reqbeam/db package

# ============================================
# DATABASE_URL (OPTIONAL - Auto-detected if not set)
# ============================================
# The database connection string. If not set, will automatically use VS Code extension database.
# Both the Web application and CLI must use the same DATABASE_URL to access the same database.

# Option 1: Use VS Code Extension Database (Default if DATABASE_URL not set)
# The scripts will automatically use the VS Code extension database if DATABASE_URL is not set
USE_VSCODE_EXTENSION_DB=true
VSCODE_EXTENSION_ID="reqbeam.reqbeam"
VSCODE_DB_FILE_NAME="reqbeam.db"

# Option 2: Manually set DATABASE_URL
# For SQLite (Development/Testing):
# DATABASE_URL="file:./prisma/dev.db"

# Option 3: Use VS Code Extension Database explicitly via DATABASE_URL
# Run: npm run db:vscode-url to get the exact URL for your system
# Then uncomment and set:
# DATABASE_URL="file:C:/Users/YourUsername/AppData/Roaming/Code/User/globalStorage/reqbeam.reqbeam/reqbeam.db"

# For PostgreSQL (Production):
# DATABASE_URL="postgresql://username:password@localhost:5432/reqbeam?schema=public"

# For MySQL:
# DATABASE_URL="mysql://username:password@localhost:3306/reqbeam"

# For SQL Server:
# DATABASE_URL="sqlserver://localhost:1433;database=reqbeam;user=username;password=password"

# ============================================
# Notes
# ============================================
# 1. The DATABASE_URL should point to the same database used by the web application
# 2. For SQLite, use relative paths from the project root or absolute paths
# 3. For remote databases, ensure network connectivity and proper credentials
# 4. Never commit the actual .env file with real credentials to version control
# 5. Copy this file to .env and update with your actual values

# ============================================
# Example for Different Environments
# ============================================

# Development (SQLite - local file)
# DATABASE_URL="file:./prisma/dev.db"

# Development (PostgreSQL - local)
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/reqbeam_dev?schema=public"

# Staging (PostgreSQL - remote)
# DATABASE_URL="postgresql://user:password@staging-db.example.com:5432/reqbeam_staging?schema=public"

# Production (PostgreSQL - remote)
# DATABASE_URL="postgresql://user:password@prod-db.example.com:5432/reqbeam_prod?schema=public"

