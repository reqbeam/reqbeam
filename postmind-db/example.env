# Database Configuration
# This file shows the required environment variables for @postmind/db package

# ============================================
# DATABASE_URL (REQUIRED)
# ============================================
# The database connection string. This must be set for the package to work.
# Both the Web application and CLI must use the same DATABASE_URL to access the same database.

# For SQLite (Development/Testing):
DATABASE_URL="file:./prisma/dev.db"

# For PostgreSQL (Production):
# DATABASE_URL="postgresql://username:password@localhost:5432/postman_clone?schema=public"

# For MySQL:
# DATABASE_URL="mysql://username:password@localhost:3306/postman_clone"

# For SQL Server:
# DATABASE_URL="sqlserver://localhost:1433;database=postman_clone;user=username;password=password"

# ============================================
# Notes
# ============================================
# 1. The DATABASE_URL should point to the same database used by the web application
# 2. For SQLite, use relative paths from the project root or absolute paths
# 3. For remote databases, ensure network connectivity and proper credentials
# 4. Never commit the actual .env file with real credentials to version control
# 5. Copy this file to .env and update with your actual values

# ============================================
# Example for Different Environments
# ============================================

# Development (SQLite - local file)
# DATABASE_URL="file:./prisma/dev.db"

# Development (PostgreSQL - local)
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/postman_clone_dev?schema=public"

# Staging (PostgreSQL - remote)
# DATABASE_URL="postgresql://user:password@staging-db.example.com:5432/postman_clone_staging?schema=public"

# Production (PostgreSQL - remote)
# DATABASE_URL="postgresql://user:password@prod-db.example.com:5432/postman_clone_prod?schema=public"

